<div class="page-header">
  <h1><%= t(:"activity.index.activities") %></h1>
</div>

<table class="table table-striped">
  <tbody>
    <% @activities.each do |activity| %>
    <tr>
      <!-- <td> -->
        <%= ActivityPresenter.new(activity, self).render_activity %>
      <!-- </td> -->
    </tr>
    <% end %>
  </tbody>
</table>
